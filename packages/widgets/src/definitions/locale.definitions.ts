/**
 * The mode that specifies the locale language of the widget
 * @remarks
 * By default, widget use browser's locale, if not defined - uses en-us
 * @public
 */
export type Locale = 'en' | 'de'; // NOTE: more languages will be added
